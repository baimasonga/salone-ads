import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const sql=readFileSync(resolve(process.cwd(),'migrations/manohub-new-project/45_platform_financial_ledger.sql'),'utf8');
describe('financial ledger',()=>{it('unifies invoices payments allocations and entries',()=>{for(const x of['commercial_invoices','commercial_payments','commercial_payment_allocations','commercial_ledger_entries'])expect(sql).toContain(x)});it('makes entries immutable',()=>{expect(sql).toContain('commercial_ledger_immutable');expect(sql).toContain('Commercial ledger entries are immutable')});it('guards payment allocation',()=>{expect(sql).toContain('record_commercial_payment');expect(sql).toContain('not public.is_platform_admin()');expect(sql).toContain('Payment exceeds outstanding balance')})});

