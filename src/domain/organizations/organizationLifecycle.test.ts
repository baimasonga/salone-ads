import { readFileSync } from 'node:fs';import { resolve } from 'node:path';import { describe,expect,it } from 'vitest';
const sql=readFileSync(resolve(process.cwd(),'migrations/manohub-new-project/49_organization_lifecycle_and_recovery.sql'),'utf8');
describe('organization lifecycle and recovery',()=>{it('supports guarded recoverable states',()=>{for(const x of["'active','suspended','recovery_pending','closed'",'recoverable_until','organization_recovery_requests'])expect(sql).toContain(x)});it('suspends commercial entitlements',()=>{expect(sql).toContain("o.status='active'");expect(sql).toContain("'Organization suspended: '")});it('provides owner recovery and ownership transfer',()=>{expect(sql).toContain('request_organization_recovery');expect(sql).toContain('transfer_organization_ownership');expect(sql).toContain("role='owner'")});it('guards administrator lifecycle actions',()=>{expect(sql).toContain('admin_transition_organization');expect(sql).toContain('admin_decide_organization_recovery');expect(sql).toContain('not public.is_platform_admin()')});it('emits auditable lifecycle events',()=>{expect(sql).toContain('organization_lifecycle_events');expect(sql).toContain('private.emit_business_event');expect(sql).toContain('public.audit_logs')})});
